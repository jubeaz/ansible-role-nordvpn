---
- name: Install packages
  pacman:
    name: "{{ item }}"
    state: present
    update_cache: true
  register: __pacman
  changed_when: __pacman.packages is defined
  loop: "{{ _nvpn_packages }}"

